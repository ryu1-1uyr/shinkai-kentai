# 深海検体増殖計画（仮題・タイトル未定）

Web ブラウザで動く、ローグライト × インクリメンタルゲームの仕様。

- プレイヤーは深海の秘密研究施設のマッドサイエンティスト
- サメを培養して突然変異させ、地上の都市にぶつけて破壊する
- 1 ラン 3〜12 分。到達深度に応じたポイントで恒久アップグレードを行い、次のランへ
- ランは必ず終わる。生産基地が逆探知され、衛星ビームで消し飛ばされる

## 1. ラン構造

1 ランは 2 つのフェーズからなり、侵略フェーズの中で深度が繰り返し進行する。

```
[ 培養フェーズ ]  60 秒固定・安全
  培養液を手動クリック → 生産施設を購入 → サメを量産
  サメ累積生産数がしきい値を超えるたびに ▶ 突然変異ドラフト（3 択）
        │
        ▼ 60 秒経過で自動的に移行（プレイヤーの任意開始は設けない）
[ 侵略フェーズ ]  深度ごとに制限時間つき
  ┌── 深度 d ──────────────────────────────┐
  │  通常建築物を n(d) 棟破壊              │
  │        ▼                               │
  │  ボス建築物が出現 → 破壊               │
  │        ▼                               │
  │  深度 d+1 へ。タイマー再設定           │
  └────────────────────────────────────────┘
  ※ 侵略中も培養液クリック・施設購入・サメ生産は並行して行える
  ※ 突然変異ドラフトも生産数トリガーで発生し続ける
        │
        ▼ 制限時間切れ
[ 逆探知 ]  衛星軌道からビームが降り、研究施設が爆散する
        │
        ▼
[ 成果報告 ]  到達深度と戦果からポイントを算出 → 恒久アップグレード
```

### 設計の中心にある一本の緊張

**建物 HP は深度ごとに指数で伸びるが、制限時間はほぼ伸びない。**

素のままではいつか必ず時間内に壊しきれなくなり、ビームが降る。
これを押し返せる手段は突然変異ドラフトだけであり、突然変異はサメの生産量に依存する。
戦闘そのものは自動なので、プレイヤーの意思決定は次の 2 つだけに絞られる。

1. **施設の購入配分** — 培養液を増やすか、サメを増やすか、投入速度を上げるか
2. **突然変異ドラフトの選択** — 同じ変異を重ねて濃くするか、別の変異を足して幅を取るか

侵略中も生産数トリガーでドラフトが飛んでくるため、
**「侵略を眺めているだけの時間」が構造的に発生しない**ようになっている。

## 2. 培養フェーズ

### 生産チェーン

| # | 資源 | 増え方 |
|---|---|---|
| 1 | 🧪 **培養液** | 手動クリック + 培養槽による自動生産 |
| 2 | 🦈 **サメ** | 繁殖槽が培養液を消費して産出。**種別ごとに在庫を持つ** |
| 3 | 💥 **戦果** | 建物破壊で加算。ラン中の主スコア |
| 4 | 🏛 **研究予算** | ラン終了時に到達深度と戦果から算出。メタ通貨 |

サメに維持コストはない。生産したら在庫に積まれ、投入するまで減らない。

### 手動クリックは最後まで意味を持つ

```
クリック 1 回の培養液 = 基礎値 × (1 + 培養槽の所持数 × 係数) × 恒久倍率
```

**培養槽を買うほどクリックが強くなる。** これにより、
自動生産が伸びても手動クリックが相対的に陳腐化しない。

- ラン序盤はクリックしないと何も始まらない（自動生産がゼロのため）
- 恒久アップグレードで「初期培養槽 +N」を買うと、開幕クリックが不要になっていく
- ただしクリック倍率も一緒に伸びるので、クリックし続ける動機は残り続ける

### 建物

| 建物 | 効果 | 初期コスト | コスト伸び率 |
|---|---|---|---|
| 培養槽 | 培養液 +1.0/s、**かつクリック倍率 +0.15** | 10 | ×1.13 |
| 給餌装置 | 培養液 +10/s | 120 | ×1.14 |
| 繁殖槽 | サメ +0.8/s | 25 | ×1.15 |
| 加速炉 | サメ生産速度 +20%（乗算） | 800 | ×1.20 |
| 射出管 | 侵略時のサメ投入速度 +15/s | 600 | ×1.18 |

コスト式: `cost(n) = base * growth^n`
サメ 1 体の生産には培養液 4 を消費する。クリック 1 回の基礎値は 2。

**初期コストは 60 秒という培養フェーズの長さに対して決める。**
初稿では繁殖槽が 500 だったため 1 個目を買えるのが開始 140 秒後になり、
培養フェーズ中にサメが 1 体も生まれなかった。
インクリメンタルの標準的なコスト感（数分〜数十分で立ち上がる想定）をそのまま持ち込むと
2 桁ずれる。**60 秒で経済が立ち上がりきるスケールに引き直すこと。**

射出管だけは侵略フェーズにしか効かないが、購入は培養フェーズから可能。
「先に撃つ準備をするか、先に弾を作るか」の判断を序盤から発生させる。

## 3. 突然変異（ドラフト）

### トリガーと適用範囲

- **サメの累積生産数**がしきい値を超えるたびに 3 枚提示 → 1 枚選択
- しきい値は等比で伸びる（例: 50 → 150 → 400 → 1,000 → …）
- 選択した変異は**それ以降に生産されるサメにのみ、一定確率で発現する**
- **すでに在庫にあるサメは変異しない**

この「遡及しない」ルールが本作で最も重要な仕掛けであり、以下が自動的に成立する。

| 生まれるもの | 理由 |
|---|---|
| サメ在庫が種別ごとのスタックになる | 変異のタイミングが在庫の構成に刻まれる |
| 在庫が自然に精鋭化していく | 弱い個体から自動出撃するため、深度が進むほど在庫の質が上がる |
| 待ち戦略が成立しない | 変異は生産数トリガーなので、生産を絞ると変異も来ない |
| ラン同士が違う顔になる | 在庫構成がそのプレイの履歴になる |

### 変異は独立にロールされ、効果は乗算される

保有している変異は、サメ 1 体が生まれるたびに**それぞれ独立に判定される**。
複数が同時に当たった個体は、両方の特性を併せ持つ**複合サメ**として生まれ、
戦闘力は各変異の倍率の**積**になる。

```
保有変異:  双頭化 20% ×8 ／ 機械化 10% ×60

生まれるサメの内訳:
  通常サメ         72%    戦闘力 1
  双頭サメ         18%    戦闘力 8
  メカサメ          8%    戦闘力 60
  メカ双頭サメ      2%    戦闘力 480     ← 複合
```

サメ 1 体あたりの期待戦闘力は次式で閉じる。

```
E[power] = Π (1 + pᵢ × (mᵢ - 1))

上の例: (1 + 0.2×7) × (1 + 0.1×59) = 2.40 × 6.90 = 16.56
```

シミュレータでこの式を直接使えるため、個体を 1 体ずつ抽選しなくてもバランス評価ができる。

### 同じ変異を重ねるとランクが上がる

3 枚の中に取得済みの変異が出た場合、選ぶとランクが上がる。
**発現率は線形に、倍率は指数的に伸ばす。**

| ランク | 発現率 | 倍率（双頭化の場合） |
|---|---|---|
| R1 | 10% | ×8 |
| R2 | 20% | ×24 |
| R3 | 30% | ×72 |

倍率の伸び率は **`rankPowerMult = 3.0`**。実測で決めた値であり、根拠は後述。

倍率も一緒に上げるのは、**そうしないと「重ねる」が数学的に常に負けるため**。
効果が乗算だと、異なる変異を取ったときだけ交差項が発生する。

```
(1 + a)(1 + b) = 1 + a + b + ab
                             ↑ 複合が当たった時のボーナス。重ね取りにはこの項がない
```

倍率を据え置く（1.0）と交差項のぶんだけ分散が勝つことは実測で確認済み
（レアリティ導入前の測定で、重ね取り E=32.9 に対し分散 E=35.9）。

さらに **§3.5 のレアリティを導入すると、分散がより強くなる**。
レアな変異は再提示されにくく重ね取りができないため、
レアを集める戦略＝分散が自動的に有利になるからである。
レアリティ導入後に掃引すると、`1.2` では全レンジで分散が勝っていた（比 0.20）。

`rankPowerMult = 3.0` を採用する。この値には数値合わせでない根拠がある。

```
双頭化を R1 → R2 に上げる価値（mult 3.0）   3.3 倍
機械化（rare）を新規で 1 枚取る価値          3.95 倍
```

**「手持ちのコモンを育てる」と「新しいレアを取る」がほぼ等価になる。**
これによりドラフトのたびに両者を天秤にかけることになる。確定後の実測は以下のとおり。

```
重ね取り一本   E[power] 3.31k   深度 8
分散一本       E[power] 3.07k   深度 8
混合（EV最大） E[power] 5.86k   深度 9   ← 両方に勝つ
```

**純粋な重ね取りでも純粋な分散でもなく、盤面を見て選ぶ混合が最も強い。**
混合ビルドの中身は `機械化R1 古代種R1 宇宙化R2 帯電化R1` で、
レジェンダリーだけを重ね取りしつつ他は散らす形になっていた。
ドラフトが意思決定として機能していることを意味する。

### 複合サメの名称は自動生成する

各変異はプレフィックスを持ち、複合サメの名前は変異 ID 順に連結して作る。

```
双頭 + 機械         → メカ双頭サメ
双頭 + 巨大 + 機械  → メガメカ双頭サメ
```

実装コストがほぼゼロで、B 級映画のタイトルがプレイヤーの手元で勝手に生成される。
在庫表がそのまま「今回のプレイで何を作ってしまったか」の記録になる。

### 変異の一覧

| 変異 | 接頭辞 | レア度 | 発現率(R1) | 倍率(R1) | 視覚チャンネル | 必要な絵 |
|---|---|---|---|---|---|---|
| **生体系**（初期から出る） | | | | | | |
| 発光 | 発光 | common | 18% | ×5 | overlay | 発光 |
| 凶暴化 | 狂乱 | common | 15% | ×4 | attach+palette | 牙 |
| 高速遊泳 | スイフト | common | 16% | ×6 | transform+palette | — |
| 白化 | アルビノ | common | 12% | ×10 | palette | — |
| 棘皮 | スパイク | common | 13% | ×9 | attach | 棘 |
| 猛毒 | ポイズン | uncommon | 8% | ×30 | palette+overlay | — |
| 双頭化 | デュアルヘッド | common | 10% | ×8 | part(頭) | 2つ目の頭 |
| 三頭化 | トリプルヘッド | rare | 5% | ×85 | part(頭) | 3つ目の頭 |
| ダブル | ダブル | common | 12% | ×6 | transform | — |
| トリプル | トリプル | uncommon | 7% | ×20 | transform | — |
| 巨大化 | 巨大 | uncommon | 8% | ×25 | transform | — |
| 菌類化 | キノコ | uncommon | 8% | ×28 | attach+palette | キノコ |
| 幽体化 | ゴースト | rare | 5% | ×95 | transform(alpha)+overlay | — |
| 屍化 | ゾンビ | rare | 5% | ×110 | palette+attach | 露出した骨 |
| 超古代 | エンシェント | legendary | 3% | ×420 | attach+palette | 背の隆起 |
| **深海系**（解禁） | | | | | | |
| 高圧適応 | 深圧 | uncommon | 9% | ×30 | palette+transform | — |
| 深淵種 | アビス | rare | 5% | ×90 | palette+overlay | 深淵の光 |
| 触手化 | タコ | rare | 5% | ×130 | part(尾) | タコ足 |
| 古代神性 | 邪神 | legendary | 3% | ×450 | attach+palette | 増えた眼 |
| **機械系**（解禁） | | | | | | |
| 装甲化 | アーマード | uncommon | 10% | ×18 | attach | 装甲板 |
| 機械化 | メカ | rare | 5% | ×60 | part(胴) | 機械の胴 |
| 帯電化 | サンダー | rare | 5% | ×120 | overlay | 電撃 |
| 機械兵装 | 機械兵装 | legendary | 3% | ×400 | attach | 砲塔 |
| **宇宙系**（解禁） | | | | | | |
| 飛行 | フライング | uncommon | 10% | ×20 | transform+palette | — |
| 隕石 | メテオ | rare | 5% | ×100 | overlay | 火の粉 |
| 宇宙適応 | コズミック | legendary | 3% | ×350 | palette+overlay | 星空 |
| エイリアン | エイリアン | legendary | 3% | ×500 | attach+palette | 触角と単眼 |
| **災害系**（解禁） | | | | | | |
| 竜巻化 | トルネード | uncommon | 9% | ×22 | transform+overlay | 渦 |
| 灼熱 | マグマ | uncommon | 8% | ×32 | palette | — |
| 氷結 | フローズン | rare | 5% | ×105 | palette+attach | 氷塊 |
| 暴風 | ストーム | rare | 5% | ×120 | overlay | 風の筋 |
| 大津波 | ツナミ | legendary | 3% | ×430 | transform+overlay | 水しぶき |

**32 種 / 5 系統。** レア度の内訳は common 7・uncommon 9・rare 10・legendary 6 で、
**rare が最も厚い層**になっている。

初期プール（生体系）は 15 種あり、legendary の超古代も含まれる。
レアリティの重みは累計生産数に連動するため、初回ランでも 0.2% で提示されうる一方、
生産が伸びると legendary 4.2% / rare 32% まで上がる。

必要な画像は **24 枚**。32 種のうち 8 種は大きさ・色・数・透明度を変えるだけで、
絵そのものを持たない。

デメリット付きの強カードも意図的に混ぜる（`[ 低予算 ] 生産コスト −50% / 戦闘力 −20%` など）。
B 級映画の改造は必ずどこかが雑であるべき。

## 3.5 レアリティ — ドラフトに時間軸を入れる

**サメ 10 匹目のドラフトと 1000 匹目のドラフトが同じ内容では、後半の緊張感が生まれない。**
そこで各変異にレア度を与え、**出現の重みを累計生産数に連動させる**。

```
weight(def, P) = RARITY[def.rarity].weight × min(1, (P / RARITY[def.rarity].scale)^0.85)

  P     … サメの累計生産数
  scale … その重みがほぼ最大に達する生産数（いつ出始めるか）
  weight… 生産が十分に伸びたときに取る枠の大きさ（最終的にどれくらい出るか）
```

| レア度 | weight | scale |
|---|---|---|
| common | 1.00 | 1 |
| uncommon | 0.70 | 120 |
| rare | 0.45 | 600 |
| legendary | 0.16 | 2000 |

指数 `0.85` は「**生産量 100 倍で出現率 50 倍**」に対応する。

### 実測された出現率（3 枚提示に 1 枚でも含まれる確率）

| 変異 | レア度 | P=10 | P=200 | P=1000 | P=10000 |
|---|---|---|---|---|---|
| 双頭化 | common | 90.0% | 59.4% | 52.1% | 50.8% |
| 巨大化 | uncommon | 12.7% | 46.0% | 39.3% | 38.1% |
| 機械化 | rare | 2.2% | 13.3% | 27.0% | 26.1% |
| エイリアン | legendary | **0.28%** | 1.7% | **5.8%** | 9.9% |

### 副次効果: 生産量がレアカードへのアクセス権になる

重みが累計生産数に連動するため、**恒久アップグレードで生産量を上げると、
レアな変異が早い段階で提示されるようになる**。

メタ進行の意味が「強くなる」だけでなく「**選択肢が増える**」になり、
「プール拡張を主軸に置く」という §5 の方針と自然に噛み合う。

### 副次効果: 重ね取りが「不運の救済」になる

レアな変異は再提示されにくいため、**レアは重ね取りできない**。
結果として役割が自然に分かれる。

- レアを引けた → 散らして複合を狙う
- レアを引けなかった → コモンを R3 まで育てて戦う

実測では、レアを一枚も引けなかったランでも
`巨大化R3` を軸に組んで到達深度 4 に届いている。
**運が悪くてもランが無駄にならない**構造になっている。

## 3.7 研究方針 — 2 本目のカード

突然変異が「検体をどう改造するか」なのに対し、研究方針は
「この実験をどう進めるか」という**施設側**の選択。
**累計で獲得した培養液**がしきい値を超えるたびに 3 枚提示される。

| 方針 | 効果（ランク 1） |
|---|---|
| 過剰採取 | 手動採取の 25% が 2 倍になる |
| 群体感知 | 在庫の検体 10 体につき手動採取 +2% |
| 触媒投与 | 突然変異の発現率 ×1.15 |
| 培養液の濃縮 | 培養液の自動生産 ×1.22 |
| 促成培養 | 検体の生産速度 ×1.20 |
| 加圧射出 | 投入速度 ×1.18 |
| 検体の再利用 | 投入した検体の 6% が在庫へ戻る |
| 実験の前倒し | 次の突然変異までの必要数 −15% |
| 廃液の再処理 | 検体 1 体の生産に要する培養液 −15% |
| 標本の保存 | ラン終了時の研究予算 +15% |

### 狙い

**1. 初回体験の穴を埋める。**
培養液はクリック 1 回目から溜まるため、**開始 3 秒で最初の選択が発生する**（実測）。
専用のチュートリアルを作らずに「カードを選ぶゲーム」だと体で覚えさせられる。

調査によれば、Day 1 リテンションは下流の全指標の上限を決め、勝負は最初の 60 秒でつく。
本作の最初の 60 秒は培養フェーズであり、
研究方針を入れる前は**何も起きないまま終わる**時間になっていた。

**2. ラン内の変数が 2 軸になる。**
それまで毎ランの違いは変異 1 本から来ていた。
2 本走らせるとビルドの組み合わせが掛け算で増える。

**3. 培養液に目的が生まれる。**
それまで培養液は検体を作るための中間素材でしかなかった。

### 群体感知について

在庫の検体数に応じて手動採取が伸びる。
これがないと**クリックは序盤しか効かない**。
調査で挙がった「Vampire Survivors は単一の動詞にすべての重みを載せている」という
指摘に対する、本作なりの回答として置いている。

### 変異ドラフトとの峻別

同じ枠に 2 種類が出るため、見分けが付かないと「またカードか」になる。
見出し・色・添える絵を分けている。

- 突然変異 … サメのスプライト / レア度の色
- 研究方針 … 設備のドット絵 / 培養液の発光色

両方が同時に条件を満たした場合は変異を先に出し、方針は次のティックまで待つ。

## 4. 侵略フェーズ

### 建物と破壊

- 各建物は HP を持つ。投入されたサメの戦闘力の合計だけ HP が減る
- **投入したサメは戦闘力にかかわらず必ず失われる**（1 体 1 回の使い切り）
- 深度 d では通常建築物を `n(d)` 棟破壊すると、ボス建築物が出現する
- ボス建築物を破壊すると深度 d+1 へ進み、制限時間が再設定される

```
深度 1  沿岸の町      民家 ×8          → ボス: 灯台
深度 2  地方都市      アパート ×10     → ボス: ショッピングモール
深度 3  県庁所在地    オフィスビル ×12 → ボス: 国際空港
深度 4  大都市        高層ビル ×14     → ボス: 海上都市
深度 5  首都          官庁街 ×16       → ボス: 国会議事堂
深度 6+ ???           （深度計の異常）  → ボス: 軌道エレベータ / 気象衛星 / …
```

深度が深くなるにつれ、標的は物理的に「上」へ向かっていく。
潜っていたはずの深度計が成層圏を指し始めるが、報告書上は正常として扱われる。

### 数値設計

```
建物 HP     H(d) = 3000 * 7.0^(d-1)
棟数        n(d) = 8 + 2*(d-1)
ボス HP     H(d) * 10
制限時間    侵略開始時に 120 秒。深度を突破するたび +15 秒（深度ごとのリセットはしない）
```

HP は指数、制限時間の継ぎ足しは線形。**この差がランを必ず終わらせる。**

制限時間を「ラン全体で 1 本、突破するたび継ぎ足し」にしたのはシミュレータの実測による。
深度ごとにリセットする方式では、進むほど時間が満タンになるためランが青天井に伸び、
1 ラン 15〜23 分に達した（後述の実測ログを参照）。

**`hpGrowth`（現在 7.0）がこのゲームの難易度ツマミである。**

研究方針を追加してラン内の強化軸が 2 本になったぶん、素の到達深度が伸びた。
5.0 のままだと 8 ラン目で天井に着いて進行が終わってしまうため、
7.0 へ引き上げて中盤を伸ばしている（掃引の実測は §10）。
この 1 つの数字を動かすだけで到達深度のレンジが決まるため、
難易度設定を実装する場合もここを触るだけで済む。

### ラン総時間は式で確定する

培養フェーズを 60 秒固定にしたことで、到達深度 D のランの総時間が閉じた式になる。

```
総時間(D) = 60 (培養) + 120 (侵略の初期持ち時間) + 15 × D
```

| 到達深度 | 総時間 |
|---|---|
| 3 | 3 分 45 秒 |
| 5 | 4 分 15 秒 |
| 10 | 5 分 30 秒 |
| 15 | 6 分 45 秒 |
| 20 | 8 分 00 秒 |

**どれだけ上達しても 1 ラン 8 分に収まり、かつ深く進むほど長くなる。**
倍速 ×2 を解禁すれば深度 20 到達でも実時間 4 分で済む。

### 操作は存在しない（完全自動戦闘）

侵略フェーズにプレイヤーの操作は一切ない。サメは自動で標的へ突撃し続ける。

- 毎秒 `R` 体が現在の標的へ自動的に突撃する（`R` = 射出管などで上昇）
- 出撃順は**戦闘力の弱い個体から**。プレイヤーによる選択は行わない
- 標的が破壊されたら自動的に次の建物へ移る

弱い順に自動出撃することで、以下が同時に解決する。

- **通常サメの死に在庫が発生しない**。雑魚は浅い深度で消費され、常に在庫が精鋭化していく
- 深度が進んだ時点で在庫に残っているのは自然と高戦闘力の個体だけになる
- 弾種の選択という操作を足さずに、在庫スタックが持つ意味を回収できる

### プレイヤーは侵略中も生産を続ける

侵略フェーズはプレイヤーが手を止める時間ではない。培養液クリック・施設購入・
サメ生産は並行して走り続け、突然変異ドラフトも生産数トリガーで飛んでくる。

つまり**侵略ビューは操作対象ではなく、生産の成果が消費されていく様子を見せる進捗表示**である。
このため侵略ビューで画面を占有してはならない。生産 UI を隠すと手が止まってしまう。
生産 UI の隣（あるいは下）に並置し、両方を同時に見られるレイアウトにする。

### ラン終了

制限時間が尽きると逆探知が完了し、衛星軌道からビームが降って研究施設が爆散する。

- 到達深度・破壊した建物数・総戦果から研究予算を算出する
- **到達深度ボーナスを乗算で乗せる**（深く進んだランほど、次のランが強くなる）
- 途中で自主的にランを終える手段は設けない（時間切れのみが終了条件）

## 5. メタ進行（恒久強化）

ラン終了時に研究予算を得て、次のラン以降に永続する強化を買う。

### 研究予算の算出

```
研究予算 = floor( √総戦果 × 0.3 × (1 + 0.5 × 突破深度) )
```

総戦果は `hpGrowth = 5.0` に引きずられて**深度ごとに約 5 倍**になる。
そのまま比例させると深度 1 つで収入が 5 倍になり、恒久強化の価値が一瞬でインフレする。
平方根で潰して深度あたり約 2.2 倍に落とし、そこへ到達深度ボーナスを乗算で乗せる。

深度を突破できなくても削ったぶんは総戦果に入るため、**「惜しかったラン」も報われる**。

係数 `0.3` は連続プレイのシミュレータで実測して決めた（§10 参照）。

### 3 系統

恒久強化は**スキルツリー**として並べる。
平坦な一覧だと「安い順に全部買う」以外の遊び方が無く、
何を伸ばしているのかがプレイヤーの中に残らない。
枝に分けて前提を付けると、序盤にどの方向へ振るかが選択になる。

| 枝 | 内容 | 並び |
|---|---|---|
| **培養** | 培養液を増やす | クリック増幅 → 培養液生産 → ダブルクリック → 温度管理 → 給餌連動 |
| **検体** | 検体を増やし強くする | 培養槽の常設 → 繁殖効率 → 検体の備蓄 → 基礎戦闘力 → 過密飼育 |
| **侵略** | 投入と破壊を伸ばす | 射出機構 → 緊急浮上 → 射出斉射 → 予備電源 → 連鎖崩壊 |
| **実験** | ドラフトを操作する | 再実験 → 早期実験 → 追加検体枠 → 試作認可 |
| **系統** | 変異の種類を解禁する | 深海系 → 機械系 → 宇宙系 → 災害系 |
| **運用** | 周回を速くする | 倍速 ×2 →（倍速 ×4 ／ 自動採取 → 定期発注 → AI 発注） |

節は 2 種類が混在する。**レベル制**（コストが等比で伸びる数値強化）と、
**買い切り**（系統解禁・特殊装備・利便性）。
レベル制の節は 1 段でも上げていれば「取得済み」となり、次の節が開く。

運用の枝だけ分岐しており、倍速と自動化のどちらを先に伸ばすかが選べる。

配線は実際に配置された位置から測って引き、前提を満たした線だけ点灯する。

### 特殊装備 — 数字ではなく挙動を変える

レベル制の数値強化とは別に、**買い切りでルールが変わる**強化を置く。
「倍率が上がるだけ」の強化ばかりだと、周回しても遊び方が変わらないため。

| 装備 | コスト | 効果 |
|---|---|---|
| ダブルクリック | 700 | 手動採取が 2 倍 |
| 再実験 | 1,500 | ドラフトを 1 ラン に 1 回だけ引き直せる |
| 温度管理 | 1,000 | 培養槽 1 個につき培養液生産 +2% |
| 給餌連動 | 1,800 | 給餌装置 1 個につき培養液生産 +3% |
| 過密飼育 | 2,600 | 繁殖槽 1 個につき検体の生産速度 +2% |
| 射出斉射 | 3,600 | 射出管 1 個につき投入速度 +3% |
| 早期実験 | 2,200 | 最初のドラフトが累計 10 体で訪れる（通常 50 体） |
| 緊急浮上 | 2,800 | ラン終了時、在庫の検体をすべて投入してから終わる |
| 予備電源 | 4,000 | 制限時間が尽きた瞬間、1 ラン に 1 回だけ +20 秒 |
| 追加検体枠 | 5,000 | ドラフトの提示が 3 枚から 4 枚になる |
| 連鎖崩壊 | 7,000 | 建物破壊時の余剰ダメージが、次の建物に 2 倍で通る |
| 試作認可 | 20,000 | 突然変異のランク上限が 3 から 4 になる |

**早期実験が構造的に最も強い。** 変異は遡及しないため、
最初の変異を 40 体ぶん早く引けると「変異が乗った状態で生産する時間」が丸ごと増え、
ラン全体の生産カーブが変異込みになる。単なる前倒しではなく複利で効く。

**連鎖崩壊は連鎖させてはならない。** 破壊のたびに余剰ダメージへ倍率を掛ける実装にしたところ、
1 つの深度に建物が 8〜26 棟あるため 2 のべき乗で暴走し、
到達深度が 12 から 28 に飛んだ（詳細は §10）。
倍率は **1 回の投入につき最初の 1 回だけ**適用する。

### 自動発注は 2 段階に分ける

全設備を自動購入させると、購入配分という数少ない意思決定が丸ごと消える。

- **定期発注**（1,200）… 設備を 1 種類だけ選んで自動購入する。何を伸ばすかの判断は残る
- **AI 発注**（12,000、定期発注が前提）… 全設備を自動購入する

### 系統解禁がローグライトとしての主軸

変異は**系統単位**で解禁する。1 変異 1 解禁にすると、
変異を 30 種まで増やしたときに解禁ボタンが 30 個並ぶことになるため。

| 系統 | 状態 | 変異 | 解禁コスト |
|---|---|---|---|
| 生体系 | 初期から | 発光 / 凶暴化 / 双頭化 / 群体化 / 巨大化 / 古代種 | — |
| 深海系 | 解禁 | 高圧適応 / 深淵種 / 触手化 / 古代神性 | 500 |
| 機械系 | 解禁 | 装甲化 / 機械化 / 帯電化 / 自律兵装 | 2,500 |
| 宇宙系 | 解禁 | 無重力 / 隕石化 / 宇宙化 / エイリアン | 15,000 |

数値強化だけだと弱いうちが苦行になるが、系統解禁は
**ゲームの語彙そのものが増える**ので、周回のたびに知らないものが出てくる。

### 生産量の強化がレアカードへのアクセス権を兼ねる

§3.5 のレアリティは累計生産数に連動しているため、
**培養液生産や繁殖効率を強化すると、レアな変異が早い段階で提示されるようになる**。

数値強化と系統解禁が意図せず噛み合っており、
メタ進行の意味が「強くなる」だけでなく「選択肢が増える」になっている。

### 初期培養槽の位置づけ

`培養槽の常設` を買い進めると開幕のクリック作業が不要になっていく。
「最初は必ずクリックさせる。上達したプレイヤーはクリックから解放される。
ただしクリックの価値自体は最後まで下がらない」を実現する中心的な強化。

（クリック 1 回の価値は培養槽の数に比例するため、
培養槽が増えるほどクリックも強くなり、陳腐化しない）

## 6. 時短とラン回転

**上達するほどランは長くなる。** 初回は深度 2 あたりで時間切れになり 2〜3 分で終わり、
変異と恒久アップグレードが積み上がるにつれて深度が伸び、ラン時間も伸びていく。
下手なうちが短いのは親切さであり、意図した設計。

そのうえで、周回のテンポを落とさないための手段を用意する。

| 手段 | 内容 | 効き方 |
|---|---|---|
| **倍速** | ×1 / ×2 / ×4 の切り替え。恒久アップグレードで解禁 | 最も効く。制限時間はゲーム内秒で管理するため、倍速でも難度は変化しない |
| **自動購入** | 「買えるものを自動で買う」トグル | 購入が単純作業になった段階で手を解放する |
| **初期ブースト** | 恒久アップグレードで初期培養槽・初期サメを持って開始 | 固定 60 秒の培養フェーズの密度が上がる |

### 「到達済み深度からスタート」は実装しない

深度だけ飛ばしても在庫が空のままなので即座に破綻するうえ、
**変異を積み上げる過程そのものが本作の中身**であり、そこを飛ばすと何も残らない。

時短は「同じことを速く」で解決し、「途中を飛ばす」では解決しない。

## 7. 技術仕様

### スタック

- TypeScript + React + Vite
- 状態管理: 外部ストア + `useSyncExternalStore`
- 描画: v1 は DOM のみ。canvas は v2
- ゲームロジックに外部ライブラリを持ち込まない

### ディレクトリ構成

```
src/
  game/           React に一切依存しない純粋なゲームロジック
    state.ts      GameState 型と初期状態
    formulas.ts   コスト・HP・戦闘力・制限時間の計算式
    tick.ts       1 tick 分の状態遷移（純関数）
    buildings.ts  生産施設の定義
    mutations.ts  突然変異の定義と効果適用
    targets.ts    建物・ボス・深度テーブル
    inventory.ts  サメ在庫（種別スタック）の操作
  store/          ゲームループと購読の橋渡し
  ui/             React コンポーネント
  meta/           セーブデータ（localStorage）
  sim/            ヘッドレスのバランスシミュレータ
```

`game/` を React から完全に切り離すのは、バランス調整をテストで回すため。
1 ラン 10 分のゲームを手で確認していると調整が永遠に終わらないので、
「この変異構成で深度いくつまで行けるか」を数千回ヘッドレスで回せる状態を最初から作る。

### サメ在庫のデータ構造

サメは単一の数値ではなく、**変異の組み合わせごとのスタック**として保持する。
複合サメが存在するため、種別は変異の集合そのものになる。ビットマスクで表現する。

```ts
type MutationMask = number    // 変異の有無をビットで保持（変異 10 種なら 10 bit）

type SharkStack = {
  mask: MutationMask          // どの変異を持つ個体か
  power: number               // 1 体あたりの戦闘力（mask から算出可能）
  count: number
}

type Inventory = Map<MutationMask, SharkStack>

/** 累計出生数。在庫と違い減らない。リザルト画面の「実験記録」に使う */
type Births = Map<MutationMask, number>
```

- `power` は `基礎戦闘力 × Π(該当変異の倍率)` で決まるため、`mask` から導出できる
- 出撃時は `power` 昇順にソートして端から消費する
- 変異 10 種をすべて取得した場合、理論上のスタック数は最大 `2^10 = 1024`

**1024 スタックはロジック上は何の問題もないが、UI にそのまま出すと破綻する。**
在庫表示は次のルールに従う。

- 並び順は **スタック合計戦力（`power × count`）の降順**
- 既定では上位 10 件を表示し、残りは折りたたむ（展開すれば全件見られる）
- **新種のサメが初めて生まれた瞬間にトースト通知を出す**
- **ラン中は現在の在庫を、リザルト画面では累計出生数（`births`）を表示する**

ラン中の在庫は培養フェーズの 60 秒で積み上がり（消費がないため）、
侵略フェーズで投入されて減っていく。実測では培養フェーズ終了時点で約 265 体に達し、
侵略の前半は生産と投入が拮抗して横ばい、射出管が揃うと一気に溶ける。
**この山が崩れていく様子そのものが在庫表示の見どころになる。**

一方でランの後半は在庫がほぼゼロで安定する（理由は次項）。
生み出した種の全リストはリザルト画面で `births` として見せる。
こちらは減らないので、**そのランで何を作ってしまったかの実験記録**になる。

### 在庫は必ずゼロに収束する（構造的な制約）

投入速度が生産速度を下回ると、ゲームは進行できなくなる。

```
弱い順に出撃 + 在庫が残り続ける
  → 変異の発現率は 100% ではないので、通常サメ（戦闘力 1）は生まれ続ける
  → 在庫がある限り、永遠に戦闘力 1 のサメを投げることになる
  → ダメージが伸びず、指数で伸びる HP に追いつけない
```

したがって **投入速度は生産速度を上回っている必要があり、在庫は定常状態でゼロになる**。
これは弱い順出撃を採用した時点で決まる制約であり、回避できない。

在庫が意味を持つのは「培養フェーズで積んだ山を侵略前半で消化するまで」の区間に限られる。
リザルトで累計出生を見せるのは、この制約を踏まえたうえで
「変な複合サメが並ぶ表」を成立させるための措置である。

トースト通知は演出ではなく機能。複合サメの名前は自動生成されるため、
プレイヤーは自分が何を作ってしまったかを在庫表を開くまで知らない。
`メガメカ双頭サメ が誕生しました` が流れてくることが、このゲームの報酬のひとつになる。

### ゲームループ

- 固定タイムステップ **20 tick/s**、`requestAnimationFrame` で駆動
- 経過時間を tick 単位に切り出して消化し、累積誤差を出さない
- タブ非アクティブ中の巻き戻しは行わない（オフライン進行なし）

### React の再描画

20 Hz で状態が変わるが、20 Hz で再レンダすると重い。

- ゲーム状態は React の外（ミュータブルなオブジェクト）に置く
- `useSyncExternalStore` で購読し、**通知は 10 Hz に間引く**
- 数値のカウントアップ演出は CSS / rAF 側で処理し、React を経由させない

### 数値の桁

1 ラン で到達する総戦果は、すべての恒久強化を積んだ状態で **約 3×10^16**（到達深度 17）。

`Number.MAX_SAFE_INTEGER`（約 9×10^15）は超えるが、**これは問題にならない**。
安全整数を超えて困るのは整数の厳密さが要る場合だけで、
本作は HP・ダメージ・在庫数のすべてが最初から小数であり、厳密な整数を必要としない。
倍精度浮動小数の相対精度は 10^16 でも有効数字 15〜16 桁が保たれ、
限界（約 1.8×10^308）までは十分な余裕がある。

したがって **break_infinity.js のような多倍長ライブラリは不要**。

制約になるのはむしろ表示側で、`src/ui/format.ts` の単位表（k / M / B / T / aa / ab / ac）が
10^24 までしか用意されていない。それを超える桁まで進行を伸ばす場合は単位を足す。

### セーブ

- localStorage に保存するのはメタ進行のみ（研究予算・購入済みアップグレード・解禁済み変異）
- ラン中の状態は保存しない。リロードでランが消えるのは仕様

## 7.5 グラフィックの差し替え方針

最終的には Cookie Clicker 程度の作り込んだ見た目にする。
そのため v1 の UI は**あとからガワを当てられること**を前提に組んである。

### 1. 見た目の値は CSS 変数に集約する

```
src/ui/styles/
  tokens.css       色・余白・角丸・文字サイズ・スプライトサイズ  ← 差し替え点
  layout.css       グリッドの配置
  components.css   各パーツの見た目
```

**TSX には色・サイズ・余白を一切書かない。** すべて `tokens.css` の CSS 変数を参照する。
テーマを変えるだけなら `tokens.css` の置き換えで完結する。

### 色が競合したら混ぜずに塗り分ける

色を変える変異は複数付きうる。順番に混ぜると後から来た色が前の色を潰し、
「氷と炎を両方持つと氷になる」という挙動になってしまう。

そこで**体を長さ方向に等分し、区画ごとに別の色を当てる**。

```
マグマ + フローズン                → 尻尾側が赤、頭側が青
マグマ + フローズン + アルビノ     → 3 等分
色変化を 5 つ持つ                  → 5 等分（ほぼ虹）
```

これにより**いくつ色変化を持っているかが見た目から数えられる**。
情報が消えず、盛るほど派手になる。

区画の並びは変異の定義順。サメは右を向いているため、
**名前の接頭辞の並び（左から）と体の並び（尻尾から頭へ）が一致する**。

### 2. 絵が入る箇所はすべて `<Sprite>` を通す

```tsx
<Sprite kind="building" id="tank" />
```

これは次の DOM を吐く。

```html
<span class="sprite" data-sprite="building:tank">
  <span class="sprite-glyph">🧪</span>
</span>
```

`data-sprite` に**安定した ID が刻まれる**ので、画像を用意したら CSS だけで差し替えられる。

**グラフィックはドット絵を前提とする。** そのため `.sprite` には
`image-rendering: var(--sprite-rendering)`（既定 `pixelated`）を最初から適用してある。
これを後から入れると全スプライトの見え方が一斉に変わってしまうため、先に入れてある。

```css
[data-sprite="building:tank"] {
  background-image: url("/assets/building/tank.png");
}
[data-sprite="building:tank"] .sprite-glyph { display: none; }
```

**コンポーネントのコードを触る必要はない。** 現在は絵文字がフォールバックとして出る。
ID の名前空間は `building:` `mutation:` `resource:` `target:` `ui:` の 5 種。

### 3. レイアウトは名前付きグリッドエリア

```css
grid-template-areas:
  "hud      hud       hud"
  "produce  invasion  stock";
```

配置を変えたいときは `layout.css` のこの定義を書き換えるだけでよく、
コンポーネントの階層を組み替える必要がない。

### 4. ロジックは UI を一切知らない

`src/game/` は React にも DOM にも依存しない。
UI を全面的に作り直しても、`src/store/gameStore.ts` の口が同じであれば
ゲームロジックには手が入らない。

## 8. v1 のスコープ

コアの面白さを検証することだけが目的。

- [ ] 培養フェーズ: 生産チェーンと施設 5 種、クリック倍率連動
- [ ] 突然変異ドラフト（変異 10〜12 種）、ランク制、複合サメと自動命名
- [ ] ビットマスクによる在庫管理と、弱い順の自動出撃
- [ ] 侵略フェーズ: 建物 HP、自動戦闘、ボス、制限時間
- [ ] 深度進行とラン終了（逆探知ビーム）
- [ ] メタ進行（数値強化 5 種 + プール拡張 3 種）
- [ ] 倍速切り替え
- [ ] DOM の数値ダッシュボード UI（生産と侵略を並置）
- [ ] ヘッドレスのバランスシミュレータ

**v1 に入れないもの**: canvas のビジュアル、演出、サウンド、リーダーボード、実績。

面白さが確認できてから v2 でビジュアルを乗せる。
`game/` と `ui/` を分離しておくのは、この乗せ替えを安く済ませるため。

## 9. 未決事項

- **培養フェーズの再訪**: ボス撃破後に短い安全時間を挟むか、シームレスに続けるか
- **変異の上限**: 1 ランで保有できる変異の種類数に上限を設けるか
- **施設の差別化**: 購入比率を変えても到達深度がほとんど変わらなかった（実測）。
  施設ごとの役割がまだ弱く、購入判断が意思決定になりきっていない
- **タイトル**

### 決定済み（判断の記録）

- **変異の発現は確率抽選とする。** 1 ランで数百〜数千体を生産するため大数の法則で均され、
  「確率で事故る」懸念は生じない。また多重変異は確率の積で表現する必要があるため、確定枠とは両立しない
- **侵略フェーズに操作は置かない。** プレイヤーの手は生産に張り付いたままにする
- **出撃順は弱い個体から固定。** これにより通常サメの死に在庫が構造的に発生しない
- **ラン中の自主終了は用意しない。** 終了条件は制限時間切れのみ
- **制限時間はラン全体で 1 本。** 深度ごとのリセット方式は実測で 15〜23 分に達したため不採用
- **`hpGrowth = 5.0`。** 初回ラン到達深度 5（4〜6 に分布）、恒久強化を積んで深度 11 の成層圏へ
- **`rankPowerMult = 3.0`。** レアリティ導入後に重ね取りと分散が拮抗し、混合が最強になる値
- **変異にレア度を設け、出現の重みを累計生産数に連動させる。** ドラフトに時間軸と運を持ち込むため
- **在庫はラン中のみ表示し、リザルトでは累計出生を見せる。** 在庫は構造上ゼロに収束するため

## 10. 実測ログ

`src/sim/` のヘッドレスシミュレータによる測定結果。
以下はすべて恒久アップグレードなしの初回ラン相当（クリック 5 回/秒、購入は目標比に従う AI）。

### 確定した初回ランの姿（恒久強化なし・60 シード）

```
到達深度        平均 4.98（分布 4〜6、標準偏差 0.62）
総時間          4 分 15 秒 前後
ドラフト回数    5 回
サメ累計生産    約 3,000 体
在庫ピーク      265 体（培養フェーズ終了時点）
```

### 運がランに与える影響

| 引けたレア度 | 出現頻度 | 平均到達深度 |
|---|---|---|
| レジェンダリーあり | 18 / 60 | 5.61 |
| レアどまり | 38 / 60 | 4.79 |
| アンコモン以下 | 4 / 60 | 4.00 |

**上振れと下振れの差は約 1.6 深度。** 運は結果を動かすが支配はしない。
実際のビルド例:

```
深度 6（上振れ）  古代種R3 帯電化R1 エイリアンR1
深度 4（下振れ）  巨大化R3 機械化R1 双頭化R1     ← レアなしでも R3 まで育てて戦えている
```

### タイマー方式の比較

| hpGrowth | 方式 | 突破深度 | 総時間 |
|---|---|---|---|
| 3.0 | runWide | 5 | 4:15 |
| 3.0 | perDepth | 10 | 14:41 |
| 2.2 | runWide | 7 | 4:45 |
| 2.2 | perDepth | 16 | 22:50 |

深度ごとにタイマーをリセットする方式は、進むほどランが際限なく伸びる。
ラン全体で 1 本にすると、係数を変えても総時間が 4 分前後に収まる。

### hpGrowth と到達深度（恒久強化の伸びしろ）

恒久強化を「サメの基礎戦闘力の倍率」で近似したときの到達深度。

```
hpGrowth |  x1    x10   x100   x1k   x10k    x1M
---------|--------------------------------------
     2.2 |   7     11     14     18     22     28
     3.0 |   5      7     10     13     15     20
     4.0 |   4      6      8     10     12     15
     6.5 |   3      4      6      7      8     11
```

`3.0` を採用した理由は、**深度 11（成層圏。深度計が壊れているという扱いになる地点）が
恒久強化 ×1000 前後で到達できる**こと。
6.5 では ×1,000,000 が必要で遠すぎ、2.2 では序盤で着いてしまいネタが安くなる。

### 生成された複合サメの例

```
双頭メガメカ古代サメ    戦闘力 480,000
メガメカ古代サメ        戦闘力  60,000
双頭メカ古代サメ        戦闘力  19,200
メカ古代サメ            戦闘力   2,400
```

**分散寄りに引いたランでは 16 種、重ね取り寄りでは 8 種が生まれた。**
分散は期待戦闘力では劣るが、生み出す種類が倍になる。
数値上の損得だけでは説明できない「見て楽しい」側の報酬が分散側にあることを意味する。

### シミュレータの実行方法

```
npm run sim            # ドラフト方針 / タイマー方式 / 購入比率の比較
node src/sim/run.ts trace     # 10 秒ごとの状態遷移をトレース
node src/sim/run.ts sweep     # rankPowerMult の掃引
node src/sim/analyze.ts       # 係数の詳細掃引とメタ進行の伸びしろ
node src/sim/species.ts       # 生成された複合サメの一覧
```

### 恒久強化を積みながらの連続プレイ（進行カーブ）

`src/sim/progress.ts` で 40 ラン連続プレイし、
毎ラン「買えるもののうち最も安いものを買い続ける」方針で強化を積んだ結果。

```
到達深度の推移:
1 → 1 → 2 → 1 → 2 → 2 → 2 → 3 → 2 → 2 → 4 → 6 → 8 → 8 → 8 → 10 → 9 → 11 → 10 → 10 …
      ↑ 3 ラン目で深度 2        ↑ 11 ラン目あたりから急加速
```

**「しばらく足踏みして、解禁を挟んで一気に伸びる」**という狙った形になっている。

報酬係数の掃引結果:

| 係数 | 深度 2 到達 | 深度 3 到達 | 深度 5 到達 |
|---|---|---|---|
| 0.1 | 13 ラン | 20 ラン | 25 ラン |
| **0.3** | **3 ラン** | **8 ラン** | **12 ラン** |
| 0.8 | 3 ラン | 5 ラン | 6 ラン |

0.1 では序盤の足踏みが長すぎ、0.8 では加速が速すぎて強化を実感する前に終わる。

**既知の頭打ち**: 20 ラン目以降は深度 9〜12 で停滞する。
数値強化が最大レベルに達し、系統もすべて解禁され終わるため。
変異と強化の追加でここは伸びる。

### 連鎖崩壊の暴走（実装時に踏んだ罠）

「建物を破壊した際の余剰ダメージが次の建物に 2 倍で通る」を、
**破壊のたびに倍率を掛ける**実装にしたところ、周回の進行が壊れた。

```
修正前: 1 → 1 → 2 → 1 → 2 → 2 → 2 → 3 → 2 → 2 → 4 → 7 → 28 → 28 → 29 …
修正後: 1 → 1 → 2 → 1 → 2 → 2 → 2 → 3 → 2 → 2 → 4 → 7 → 10 → 12 → 12 → 13 …
```

1 つの深度に通常建築物が 8〜26 棟あるため、破壊ごとに倍率が掛かると
最大 2^26 倍に達し、HP の伸び（深度あたり 5 倍）を一瞬で突き抜ける。
到達深度が 28 まで飛び、総戦果も 10^28 に達して設計上の桁上限（10^15）を大きく超えた。

**倍率は 1 回の投入につき最初の 1 回だけ適用する。**
修正後は最大の総戦果が 4.9×10^13 に収まり、桁上限の内側に入った。

### 特殊装備をすべて投入したあとの進行カーブ

```
1 → 1 → 2 → 1 → 2 → 2 → 2 → 3 → 2 → 2 → 4 → 7 → 10 → 12 → 12 → 13 → 12 → 13 → 15 → 12 …
```

序盤の目標（初回は深度 1、3 ラン目で深度 2）を維持したまま、
頭打ちが深度 9〜12 から **12〜15** へ伸びた。
深度 15 付近が現在の設計上の上限（総戦果が 10^15 に接近するため）。

### 開発用パネル

`src/ui/components/DebugPanel.tsx` は `import.meta.env.DEV` で囲ってあり、
本番ビルドではコンポーネントごと消える。
全解禁・予算付与・培養フェーズのスキップ・ラン終了・メタのリセットができる。
