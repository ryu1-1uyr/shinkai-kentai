# タスク

## バランス調整（プレイ可能になってから）

実際に遊べる状態にしてから、体感を見ながら詰める。
それまでは暫定値で進める。数値はすべて `src/game/config.ts` に集約してあり、
`src/sim/` のヘッドレスシミュレータで掃引できる。

### 目標とする進行カーブ

| 状況 | 到達深度 |
|---|---|
| 初回ラン | **1** |
| 2〜5 ラン目 | **2** |
| 以降 | 恒久アップグレードで加速度的に伸びる |

恒久アップグレードを取るほど、サメの生産量が増え、
生産量が増えるとレアな突然変異が引きやすくなり（§3.5 のレアリティは生産数連動）、
結果として一気に進みやすくなる——という加速感を作りたい。

### 現状（暫定値）

```
baseHp        3000     初回ラン平均深度 1.25（20 シード）
hpGrowth      5.0      難易度ツマミ。ここだけで到達深度レンジが決まる
rankPowerMult 3.0      重ね取りと分散が拮抗する値
```

### 詰めきれていない点

- [x] ~~恒久アップグレードがシミュレータに入っていない~~ → `src/sim/progress.ts` で連続プレイを検証できるようにした
- [ ] **頭打ちが深度 14〜17。** 設備連動の追加でさらに伸びたが、まだ止まる。
      桁の余裕は十分あるので（倍精度の限界まで遠い）、変異と強化を足せばまだ伸ばせる
- [ ] **変異の追加（最優先の content 作業）。** 現在 18 種。目標 30 種。
      新しい系統を足すか、既存系統に厚みを持たせるかは要検討。**変異は最大 31 種まで**
      （在庫キーがビットマスクのため。超える場合は `mutations.ts` のキー表現を差し替える）
- [ ] **初回ランの体感の短さ。** 深度 1 で終わると総時間 3 分程度。短すぎないか実機で確認
- [ ] **施設の差別化が弱い。** 購入比率を変えても到達深度がほとんど変わらない（実測済み）。
      施設ごとの役割づけを見直す
- [ ] **変異のデメリット付きカードが未実装。** `[ 低予算 ]` などを入れて選択に幅を出す
- [ ] **表示単位の追加。** `src/ui/format.ts` の単位表が 10^24 までしかない。
      それ以上の桁まで進行を伸ばす場合は単位を足す（多倍長ライブラリ自体は不要）

## グラフィック（v2）

**方針: ドット絵ベース。** `.sprite` には `image-rendering: pixelated` を適用済みなので、
画像を用意して `data-sprite` セレクタに `background-image` を当てるだけで差し替わる。

必要なスプライトの名前空間:

```
building:  tank / feeder / breeder / accelerator / launcher
mutation:  twinHead / frenzy / swarm / giant / ancient / mecha / volt / alien / cosmic
resource:  culture / shark / score
target:    normal / boss
ui:        beam
```

- [ ] ドット絵スプライトの作成
- [ ] **突撃ビュワー** — 深度表示の下に、建物のグラフィックへサメが突っ込んでいく様子を出す。
      ロジックは既に「毎秒 N 体が出撃して HP を削る」で確定しているため、
      表示側は `launchRate` と `currentHp` を読んでアニメーションさせるだけでよい。
      レイアウトは `layout.css` の `invasion` エリア内に収める
- [ ] 演出・サウンド

## 未着手

- [ ] タイトル決め
- [ ] アクセシビリティ（ボタンに aria-label。カード・アイコンボタンが読み上げで空になる）
